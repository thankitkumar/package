
'use client';
// This file can be a simple re-export or contain additional logic if needed in the future.
// For now, it just re-exports from the context file.
export { useFeatureFlags } from '@/contexts/feature-flag-context';
export type { FeatureFlag, RolloutCondition } from '@/contexts/feature-flag-context';

